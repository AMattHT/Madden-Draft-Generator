# Dependencies
node_modules/

# Build output
dist/
build/
*.tsbuildinfo

# Runtime nflverse downloads (re-fetched on demand by CombineService/TeamService)
server/cache/

# Logs & debug
*.log
M26Writer_debug.log

# Env / secrets
.env
.env.*

# Local tooling / editor
.claude/
.impeccable/
.DS_Store
Thumbs.db

# Extracted Madden disc headshots (167 MB, 8,101 files). The installer bundles
# them, but they are not kept in git — rebuild with:
#   npx tsx server/scripts/build-retro-headshot-pack.ts
# from the per-disc output in ../headshots/madden<year>-ps2|ps3/.
server/data/retro-portraits/
